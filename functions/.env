# Non-secret function config (safe to commit). Secrets live in .secret.local
# locally and in Google Secret Manager in production — never here.

# Toggle Stripe automatic tax on Checkout Sessions. Default ON.
# Set to false to test checkout without a tax line (no Stripe Tax setup needed).
AUTOMATIC_TAX_ENABLED=false
