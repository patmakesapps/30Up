node_modules
dist
dist-ssr
*.local

# Local environment / secrets
.env
.env.*
!.env.example
# functions/.env holds only NON-secret function params (safe to commit).
!functions/.env

# Stripe / server-side secrets — NEVER commit these
stripe_secrets.txt
functions/.secret.local

# Admin bootstrap — local utility + its service-account key. Never commit either:
# the key is a credential, and the script stays a local-only admin tool.
setAdmin.key.json
setAdmin.mjs

# Editor / tooling local state
.claude/
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local dev logs
vite-dev.log
*.log

# Firebase deploy cache
.firebase/
